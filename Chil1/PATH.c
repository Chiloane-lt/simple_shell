#include <stdio.h>
#include <stdlib.h>

extern char **environ;
 
int printPATH(void)
{
	    const char **environment = environ;
	
	    while (*environment != NULL)
	    {
		    if (ifff)
	    }

	    return (0);
}
